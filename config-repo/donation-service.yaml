# ============================================
# DONATION SERVICE CONFIGURATION
# ============================================
spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true

server:
  port: 8081

payment:
  service:
    url: http://payment-service:8082

logging:
  level:
    com.yukdonation.donation: DEBUG